name: Build Executables

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: windows-latest
            artifact: PinballScores-Windows
            ext: .exe
          - os: ubuntu-latest
            artifact: PinballScores-Linux
            ext: ""
          - os: macos-latest
            artifact: PinballScores-macOS
            ext: ""

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt pyinstaller

      - name: Build executable
        run: pyinstaller PinballScores.spec

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: dist/PinballScores${{ matrix.ext }}

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')

    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare release files
        run: |
          mkdir -p release
          cp artifacts/PinballScores-Windows/PinballScores.exe release/PinballScores-Windows.exe
          cp artifacts/PinballScores-Linux/PinballScores release/PinballScores-Linux
          cp artifacts/PinballScores-macOS/PinballScores release/PinballScores-macOS
          chmod +x release/PinballScores-Linux release/PinballScores-macOS

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            release/PinballScores-Windows.exe
            release/PinballScores-Linux
            release/PinballScores-macOS
          generate_release_notes: true
